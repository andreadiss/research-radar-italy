export function OAuthCallback() {
  return (
    <main className="shell auth-callback-shell">
      <section className="auth-modal">
        <h1>Login Google</h1>
        <p>Login Google temporaneamente non disponibile nella versione statica.</p>
      </section>
    </main>
  );
}
